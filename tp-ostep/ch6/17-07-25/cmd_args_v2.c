#include<stdio.h>

int main(int argc, char* argv[]){
    // char name[50];
    // printf("Enter your name: ");
    // fgets(name, sizeof(name), stdin);

    printf("argv[1,2][s]: [%s,%s]", argv[1],argv[2]);
    // printf("G'day %s [file: '%s']", argv[1], argv[0]);

    return 0;
}
